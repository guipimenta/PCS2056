program 
begin
  def integer fibonacci (integer #n)
  begin
    declare integer #i, #a, #b, #fib;

    #i = 0;
    #a = 0;
    #b = 1;

    while #i < #n then
      #fib = #a + #b;
      if #a == #b then
        output "hello";
      fend
      #a = #b;
      #b = #fib;
    wend

    return #fib;
  end
end